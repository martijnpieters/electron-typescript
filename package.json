{
  "name": "electron-typescript",
  "productName": "Electron + TypeScript",
  "version": "0.0.1",
  "description": "Electron + Typescript boilerplate",
  "repository": {
    "type": "git",
    "url": "https://github.com/martijnpieters/electron-typescript.git"
  },
  "main": "src/electron/main.js",
  "scripts": {
    "clean": "find ./src -type f -name *.js | xargs rm && rm -rf ./target",
    "clear": "npm run clean",
    "build": "tsc",
    "package": "npm run clean && npm run build && electron-packager . --out ./target",
    "package-all": "npm run clean && npm run build && electron-packager . --all --out ./target",
    "postinstall": "typings install",
    "preinstall": "npm install typescript typings -g",
    "prestart": "npm run build",
    "start": "electron src/electron/main.js"
  },
  "author": "Martijn Pieters",
  "license": "MIT",
  "devDependencies": {
    "electron-packager": "^8.2.0",
    "electron-prebuilt": "^1.4.5"
  }
}
